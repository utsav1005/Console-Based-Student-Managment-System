# Console-Based-Student-Managment-System
Developed a Java console application to manage student records using the Collection Framework (ArrayList, Iterator). Implemented complete CRUD operations (Add, View, Update, Delete) with proper data validation and duplicate ID checks.
